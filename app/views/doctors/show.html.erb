<ul>
<% @doctor.appointments.each do |appoint| %>
   <li> <%= appoint.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%=link_to appoint.patient.name, patient_path(appoint.patient) %>
<% end %>
</ul>